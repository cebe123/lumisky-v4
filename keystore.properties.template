# Copy this file to keystore.properties and fill it locally.
# Do not commit keystore.properties or the .jks/.keystore file.
storeFile=path/to/lumisky-upload.jks
storePassword=change-me
keyAlias=lumisky-upload
keyPassword=change-me
